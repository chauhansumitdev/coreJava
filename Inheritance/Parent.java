package Inheritance;

// extends super 
// single multilevel hierarchical multiple(interfaces)- it is when the child class extends more than one parent. cannot use two extends but can use implements A,B
public class Parent {
    void advice(){
        System.out.println("Exlain me the domino effect!");
    }
}
